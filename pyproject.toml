[project]
name = "michael-page"
version = "0.1.0"
description = "Caderno tematico DIO/NotebookLM + protótipo de IA para o gap de preparação de candidatos da Michael Page"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.120.2",
    "youtube-transcript-api>=1.2.4",
]
